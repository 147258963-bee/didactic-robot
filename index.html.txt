<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Geo Blocking | Expertos en Landing Pages y Software</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        :root {
            --bg-dark: #0a0033;
            --accent-gradient: linear-gradient(45deg, #ff8c00, #e91e63);
            --cyan: #00e5ff;
            --text-light: #ffffff;
            --wa-green: #25d366;
        }

        body {
            margin: 0;
            font-family: 'Segoe UI', sans-serif;
            background-color: var(--bg-dark);
            color: var(--text-light);
            line-height: 1.6;
        }

        /* Cabecera con Logo */
        header {
            padding: 15px 5%;
            display: flex;
            align-items: center;
            gap: 20px;
            background: rgba(0, 0, 0, 0.6);
            backdrop-filter: blur(10px);
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .logo-img { height: 50px; border-radius: 5px; }
        .brand-name { font-weight: bold; color: var(--cyan); font-size: 1.5rem; letter-spacing: 1px; }

        /* Sección de Landing Pages y Servicios */
        .services {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 25px;
            padding: 60px 5%;
            max-width: 1200px;
            margin: 0 auto;
        }

        .card {
            background: rgba(255, 255, 255, 0.03);
            padding: 40px;
            border-radius: 20px;
            text-align: center;
            border: 1px solid rgba(255, 255, 255, 0.1);
            transition: 0.3s;
        }

        .card:hover { transform: translateY(-10px); border-color: var(--cyan); }
        .card i { font-size: 3rem; color: var(--cyan); margin-bottom: 20px; display: block; }

        /* Botón de WhatsApp Flotante */
        .whatsapp-float {
            position: fixed;
            bottom: 30px;
            right: 30px;
            background-color: var(--wa-green);
            color: white;
            width: 65px;
            height: 65px;
            border-radius: 50px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 35px;
            text-decoration: none;
            box-shadow: 0 4px 15px rgba(0,0,0,0.4);
            z-index: 2000;
            transition: 0.3s;
        }
        .whatsapp-float:hover { transform: scale(1.1); background-color: #128C7E; }

        /* Formulario Directo a Gmail */
        .contact-box {
            max-width: 550px;
            margin: 40px auto;
            background: rgba(0,0,0,0.3);
            padding: 30px;
            border-radius: 25px;
            border: 1px solid rgba(255,255,255,0.05);
        }

        .contact-form input, .contact-form textarea {
            width: 100%;
            padding: 15px;
            margin-bottom: 15px;
            border-radius: 10px;
            border: none;
            background: rgba(255,255,255,0.1);
            color: white;
            box-sizing: border-box;
        }

        .btn-send {
            width: 100%;
            padding: 18px;
            border-radius: 10px;
            border: none;
            background: var(--accent-gradient);
            color: white;
            font-weight: bold;
            font-size: 1.1rem;
            cursor: pointer;
        }
    </style>
</head>
<body>

    <a href="https://wa.me/56949818809?text=Hola%20Geo%20Blocking,%20necesito%20cotizar%20una%20Landing%20Page." class="whatsapp-float" target="_blank">
        <i class="fab fa-whatsapp"></i>
    </a>

    <header>
        <img src="logo.png" alt="Geo Blocking Logo" class="logo-img"> <span class="brand-name">GEO BLOCKING</span>
    </header>

    <section style="text-align: center; padding: 80px 20px;">
        <h1 style="font-size: 3rem; background: var(--accent-gradient); -webkit-background-clip: text; -webkit-text-fill-color: transparent;">Creamos tu Landing Page</h1>
        <p style="font-size: 1.2rem; opacity: 0.8;">Diseño web, Apps móviles y Automatización inteligente 🚀</p>
    </section>

    <section class="services">
        <div class="card">
            <i class="fas fa-laptop-code"></i>
            <h3>Landing Pages</h3>
            <p>Diseños modernos y optimizados para captar clientes en segundos.</p>
        </div>
        <div class="card">
            <i class="fas fa-mobile-alt"></i>
            <h3>Apps Móviles</h3>
            <p>Desarrollo premium para iOS y Android nativo.</p>
        </div>
        <div class="card">
            <i class="fas fa-robot"></i>
            <h3>Chatbots</h3>
            <p>Automatización de ventas y atención 24/7.</p>
        </div>
        <div class="card">
            <i class="fas fa-fingerprint"></i>
            <h3>Biometría</h3>
            <p>Seguridad de acceso facial y dactilar para empresas.</p>
        </div>
    </section>

    <section class="contact-box">
        <h2 style="text-align: center;">Solicitar Presupuesto 📩</h2>
        <form action="https://formspree.io/f/geobloking@gmail.com" method="POST" class="contact-form">
            <input type="text" name="nombre" placeholder="Nombre Completo" required>
            <input type="email" name="email" placeholder="Correo Electrónico" required>
            <textarea name="proyecto" rows="4" placeholder="Háblanos de tu landing page o proyecto..." required></textarea>
            <button type="submit" class="btn-send">Enviar Solicitud a Geo Blocking</button>
        </form>
    </section>

    <footer style="text-align: center; padding: 40px; background: #05001a;">
        <p>geobloking@gmail.com | +56 9 4981 8809</p>
        <p style="opacity: 0.4; font-size: 0.8rem;">© 2024 Geo Blocking - Soluciones en Software</p>
    </footer>

</body>
</html>
